<!DOCTYPE html>

<head>

</head>

<body style="text-align: center">
    <div style="font-size:30px">登録した物質一覧</div>
    <div>
        <ul>
            <% @materials.each do |material| %>
            <li style="font-weight: bold"><%= material.name %>:<%= material.amount_of_substance %></li>
            <% end %>
            <li></li>
        </ul>
    </div>
</body>